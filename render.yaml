# Render.com: New + Blueprint → bu repo'yu seçin; ortam değişkenlerini Dashboard'dan ekleyin.
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: kurumsal-tedarikci
    runtime: node
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /
